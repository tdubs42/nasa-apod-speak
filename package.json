{
  "name": "nasa-apod-time-capsule",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "migrate": "knex migrate:latest",
    "rollback": "knex migrate:rollback",
    "seed": "knex seed:run",
    "resetdb": "knex migrate:rollback && knex migrate:latest && knex seed:run",
    "hmigrate": "heroku run knex migrate:latest -a apod-time-capsule",
    "hrollbackh": "heroku run knex migrate:rollback -a apod-time-capsule",
    "hdatabase": "heroku pg:psql -a apod-time-capsule",
    "hseed": "heroku run knex seed:run -a apod-time-capsule",
    "test": "cross-env NODE_ENV=testing jest --watchAll --verbose --runInBand",
    "hpush": "git push heroku main"
  },
  "author": "tdubs42",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^14.2.0",
    "express": "^4.17.2",
    "helmet": "^5.0.1",
    "knex": "^1.0.1",
    "pg": "^8.7.1"
  },
  "devDependencies": {
    "@types/jest": "^27.4.0",
    "eslint": "^8.7.0",
    "jest": "^27.4.7",
    "knex-cleaner": "^1.3.1",
    "nodemon": "^2.0.15"
  }
}
